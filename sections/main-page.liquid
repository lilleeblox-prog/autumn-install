<section class="mtw-section">
  <div class="mtw-container">
    <h1 class="mtw-h1">{{ page.title }}</h1>
    <div class="mtw-prose">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Page — Default",
  "settings": []
}
{% endschema %}
